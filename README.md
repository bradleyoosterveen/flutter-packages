# Commands

`fvm dart run melos bootstrap`

`fvm dart create -t package packages/ui`

`fvm dart test packages --reporter=expanded`

`fvm dart run index_generator`

`fvm dart format . && fvm dart fix --apply`